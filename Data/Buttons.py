from utils.math_utils import *
from ui.main_menu import *

buttons = {
        "Play": (1, WIDTH / 2, HEIGHT / 2 - 20, 50, 100, "Play"),
        "Pause": (1, WIDTH / 2, HEIGHT / 2 - 100, 50, 100, "Pause"),
        "Stop": (1, WIDTH / 2, HEIGHT / 2 + 80, 50, 100, "Stop"),
    }